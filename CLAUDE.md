# 魔物觀測者 Remake (Rust + WebAssembly)

重製 Cross Gate（魔力寶貝）寵物檔次計算工具「噬生・魔物觀測者」v3.12。
原程式為 2011 年的 Borland Delphi 32-bit Windows 程式，本專案以 **Rust 編成 WebAssembly
的純網頁版**重建其**計算邏輯**與**介面／操作邏輯**，部署到 GitHub Pages。

> **曾經是 Tauri 桌面版，已經不是了。** 計算核心（`petcalc` / `petcore`）原封不動，
> 換掉的只有最外層的殼：`src-tauri/` 刪掉，改成 `crates/petweb`（wasm-bindgen），
> 前端沿用同一組 `invoke(cmd, args)` 介面，所以 `src/` 上面那幾層一行都沒動。
> 沒有後端、沒有伺服器、沒有安裝檔 —— 開網頁就跑。

> ⚠️ **這個 repo 只放重製出來的東西。** 原程式的執行檔、反編譯結果、函式位址、
> 抽資源的腳本都在 `orig/` 與 `re/`，兩個目錄都被 `.gitignore` 擋掉。
> 「我們怎麼知道公式長這樣」的推導過程在本機的 **`re/REVERSE.md`**；
> **結論在這份文件裡**，這份才是規格。要改公式先讀 §3。

---

## 1. 專案定位

|              |                                                          |
| ------------ | -------------------------------------------------------- |
| 原程式       | 噬生・魔物觀測者 v3.12（Delphi PE32，2011-12-19）        |
| 計算邏輯參考 | https://github.com/tony1223/cg-pet-calc → `lib/Pets.mjs` |
| 圖鑑來源     | cg-originmood-dc 的「專屬寵物」CSV（見 §2.1）            |

### 1.1 專案結構

```
crates/petcalc/   純計算：成長係數表、正算、推算、機率統計、寵物搜尋（無 I/O，無平台相依）
crates/petdata/   資料解析：originmood CSV、（歷史用的）spetdata.ini、內建圖鑑表
crates/petcore/   應用層：DTO、圖鑑疊層、invoke 派送。**平台無關**，靠 Store trait 存自製寵物
crates/petweb/    wasm 外殼（wasm-bindgen）。只搬位元組，不含任何邏輯
tools/gen/        對照向量產生器（本機跑，產物進版控）
src/              前端（Preact + htm，無 JSX）。api.js ↔ worker.js ↔ wasm
```

計算跑在 **Web Worker** 裡。Worker 拿不到 `localStorage`（規格如此），所以自製寵物的
寫入走「wasm 標記 dirty → 主執行緒取走 JSON → 寫 localStorage」。

> Worker 當初的理由寫的是「推算會跑好幾秒」，那句**沒站住**：實測所有真實案例
> （含 `field_cases.rs` 那七個）都在 40 ms 內收工，擺主執行緒也不會卡。
> 留著是因為它已經在跑而且沒有壞處 —— 推算的上界終究是 3125 組檔次 × 加點 ×
> 1001 組隨機檔，剪枝失效時仍有可能拖長，而 worker 讓那個壞情況只是變慢不是凍住。

### 1.2 為什麼要移植而不是直接用 cg-pet-calc

cg-pet-calc 已能算「正解檔次」，但**野寵推算**與**加點推算**這兩塊，魔物觀測者做得明顯更好：

1. ~~**成長係數表被截斷且有錯**~~ → ✅ **已照魔觀修好**（見 §3.2）。
   cg-pet-calc 原本只到 index 52 且 51/52 值錯誤，已補成完整 111 筆。
   這一項兩邊現在**完全一致**，反而讓 cg-pet-calc 可以當作全檔次範圍的差分基準。
2. **野寵**沒有手動加點，但有「入手等級」概念：野寵在 N 級被抓到，1→N 的等級點是系統
   隨機配的，不是玩家配的。這使得解空間與家寵完全不同，需要獨立的推算模式。
3. **加點模式**：原程式提供 智／野／無／體／力／防／敏／魔 八種運算模式，以及
   指定加點／混加／暴點加點 三種加點方式，大幅縮小解空間。
4. **操作流程**清楚好用：主視窗一次到位，側欄五個分頁漸進展開進階選項。
5. ⭐ **檔次比對的容差**（見 §3.4）— 這是最關鍵的一點。cg-pet-calc 原本只有整數精確
   列舉，野寵在高等級常常直接「無解」；原程式會針對成長係數表不規則的地方放寬取整
   邊界，所以推得出來。這一項**也已經加回 cg-pet-calc**（`opts.mode = 'observer'`），
   兩邊逐案例解數相同。
6. **機率統計**（見 §3.5）— 原程式會對候選解加權統計，輸出「符合機率 %」，
   cg-pet-calc 只有列舉，沒有機率量化。
7. **cg-pet-calc 會丟掉精神／回復**：`Stat` 建構子是 `(lvl, hp, mp, attack, defend, agi)`
   只有 6 個參數，但 `calcRealNum()` 傳了 7 個值進去 —— 算出來的 `wis` / `res`
   直接被丟棄。原程式是有顯示這兩項的，移植版必須保留。

---

## 2. 資料格式

### 2.1 `專屬寵物.csv` — 上游圖鑑（現行）

**UTF-8 with BOM**、1069 筆。**開網頁時直接抓，不進版控** —— 要的是「永遠跟著
上游走」，不是某次轉檔的快照。抓不到就只剩內建表（`bootstrap()` 吞掉這一步的錯誤，
程式照樣能算，只是少了幾百隻）。

`api.js` 的 `CATALOG_URLS` **依序試兩條，第一條成功就用**。那不是兩份資料，
是同一個檔案的兩個出口（站台那條是 build 時原樣吐出來的，逐位元相同）：

| #   | 出口                                                      | 說明                                                                      |
| --- | --------------------------------------------------------- | ------------------------------------------------------------------------- |
| 1   | `cg-originmood-dc.github.io/data/專屬寵物.csv`            | 站台發布的。本站也掛在那個網域底下時**同源**，不吃 CORS                   |
| 2   | `raw.githubusercontent.com/.../content/data/專屬寵物.csv` | 來源檔本人。站台還沒部署新版時的退路，回 `Access-Control-Allow-Origin: *` |

> 出口 1 是**這邊要求對方加的**：站台（Astro，不是 Hugo）本來只在 build 時
> 把 CSV 讀進來渲染成 HTML 表格，CSV 自己不會進 `dist/`。加的是一條靜態端點
> `src/pages/data/[name].csv.ts`，從 `content/data/` 讀出來原樣送。
> **刻意不用複製到 `public/` 的作法** —— 那會變成同一份資料存在兩個地方，
> 編輯者改了來源忘了同步副本，外部拿到舊資料而且不會有任何東西報錯。

欄位**按表頭名字找，不按位置**（`petdata::parse_originmood`），上游加欄不會炸
—— 實際上它就有好幾欄我們不看的：

| 表頭                       | 用途                                            |
| -------------------------- | ----------------------------------------------- |
| `名稱`                     | **疊層的鍵**（見 §2.3）                         |
| `種族`                     | 名字直接對 `RACE_NAMES`，認不得就落到 `其他`(9) |
| `體力 力量 防禦 速度 魔法` | 五項成長檔次                                    |
| `技格`                     | 可學技能格數                                    |
| `屬性`                     | 形如 `地10`／`水5火5`，×10 之後總和必須是 100   |
| `總檔`                     | 五項檔次的和。**只用來驗，不存**                |
| `技能` `image` `任務用途`  | 站台自己用的，移植版不看                        |

`能力倍率` CSV 裡沒有 —— 全圖鑑恆為 20，所以用 `DEFAULT_BPRATE = 0.2`。
`編號` 也沒有：那是原程式動畫資源（`C{N}`）的鍵，這份資料集不帶它。

表頭第一欄帶 BOM（`\u{feff}名稱`），`parse_originmood` 會先 `trim_start_matches`
吃掉 —— 不吃的話「按名字找欄位」第一欄就對不上，整份解析失敗。

> 上游有一筆髒資料：`暴雪魔獸` 的屬性是 `水10(水5火5)`。解析器只取 `(` 之前那段
> —— 括號裡是變身後的屬性，不是同一隻的資料。

#### 種族名稱以這份為準

```
0 野獸系 / 1 不死系 / 2 飛行系 / 3 昆蟲系 / 4 植物系
5 特殊系 / 6 金屬系 / 7 龍系   / 8 人形系 / 9 其他（種族不明，RACE_UNKNOWN）
```

⚠️ **碼沒變，名字換了。** 舊的 INI 用的是另一套叫法，而且**猜不出來**：

| 碼  | 舊（INI）  | 新（originmood） |
| --- | ---------- | ---------------- |
| 1   | 死靈系     | 不死系           |
| 2   | **邪魔系** | **飛行系**       |
| 5   | 能源系     | 特殊系           |
| 6   | **各種系** | **金屬系**       |
| 7   | 龍族系     | 龍系             |
| 8   | 人族系     | 人形系           |

對照不是猜的：把 1069 筆 CSV 按**名字**跟當時的圖鑑 join，碼與碼是完美 1:1、
零衝突。碼 9 原本在 INI 世界叫「特殊系」（只出現在內建表的兩筆），現在被
`RACE_UNKNOWN` 佔用當「種族不明」的容器，那兩筆已改成空種族。

編輯畫面的種族下拉是 `race_names.slice(0, unknown_race)`（切掉「其他」——
那是容器不是種族），由 `dto.rs::the_race_list_is_indexed_by_code_with_unknown_last` 釘住。

### 2.1.1 `spetdata.ini` — 舊上游（已退場）

`petdata::parse_spetdata` 還在，但**不在執行路徑上**了。留著的理由：格式記在這裡，
而且誰手上有原程式安裝目錄的那份 INI 時，這是唯一讀得回來的東西。

- **編碼：UTF-16-LE with BOM**（`FF FE`），CRLF
- 來源：`https://cg-static.tonyq.org/pet/spetdata.ini`

```ini
[自製寵物]
編號1069=小白龍,20 36 9 42 16 20 7 0 100 0 0 123
[龍的砂時計]
備份日期=2026/4/26
[檢查資料]
版本=魔物觀測者 v3.12
```

記錄格式 `編號{N}={名稱},{12 個空白分隔整數}`：

| #    | 欄位        | 範圍               | 說明                            |
| ---- | ----------- | ------------------ | ------------------------------- |
| 0    | 體力 HP     | 3–52               | 成長檔次                        |
| 1    | 力量 ATK    | 4–52               | 成長檔次                        |
| 2    | 強度 DEF    | 4–48               | 成長檔次                        |
| 3    | 速度 AGI    | 4–50               | 成長檔次                        |
| 4    | 魔法 MP     | 3–50               | 成長檔次                        |
| 5    | 能力倍率    | 恆為 20            | ×100 表示，20 → `bprate = 0.20` |
| 6    | 種族        | 0–8                | 見下表                          |
| 7–10 | 地/水/火/風 | 0–100（10 的倍數） | 四項總和恆為 100                |
| 11   | ⚠️ 意義換過 | 見下               | 舊檔 6–10；現行上游 50–127      |

種族碼跟 §2.1 同一套（碼相同、名字是舊叫法）。

#### ✅ 第 12 欄（index 11）之謎已結案

| 來源                             | 筆數     | 第 12 欄   |
| -------------------------------- | -------- | ---------- |
| 2023 年的 `spetdata.ini`         | 499      | 6–10       |
| v3.12 安裝目錄的 `spetdata.ini`  | 791      | 6–10       |
| v3.12 安裝目錄的 `2spetdata.ini` | 522      | 6–10       |
| 最後一版上游 INI                 | **1069** | **50–127** |

舊世代的 6–10 是「可學技能格數」。新世代那串**是五項檔次的和** ——
拿最後一版 INI 跟 originmood 的 CSV 逐筆對，第 12 欄 == `總檔` 是 **1069/1069**，
== `技格` 是 **0/1069**。所以上游在某一版把這欄的意義從技能格換成了檔次總和。

`petdata::skill_slots` 當初選擇「超過 `MAX_SKILL_SLOTS`（20）就回 `None`」而不是硬猜，
**事後看是對的**：真的猜下去，圖鑑會顯示「技能格 125」、寵物搜尋的技能下限會拿去
跟檔次總和比。現在技能格改由 CSV 的 `技格` 欄直接給，這個函式只剩舊 INI 在用。

版本之間**是整批重新編號的，同一個 `編號N` 在不同版本指向不同寵物** — 不可混用。

### 2.2 `custom_pets.json` — 自製寵物（**原程式沒有這個檔**）

原程式把自製寵物寫回 `spetdata.ini` 的 `[自製寵物]` 段（走 `WritePrivateProfileStringW`，
所以是 UTF-16-LE），編號由 `1` 掃到 `9999` 找第一個空號自動配上去。

移植版**不寫 `spetdata.ini`**，理由由弱到強：

1. 寫檔一律 UTF-8，而 INI 的既定格式是 UTF-16-LE — 照寫下去原程式就讀不回來
2. `spetdata.ini` 是自動更新下來的（整份蓋掉），寫在那裡的東西下次更新就沒了
3. 手上那幾份 INI 是逆向用的參考樣本，覆寫它等於毀掉物證

改成存成一份獨立的 `custom_pets.json`（UTF-8 無 BOM）：

```json
{ "version": 1, "pets": [ { "name": "我抓的怪", "race": 0, "grow": [30,30,30,30,30], "bprate": 0.2 } ] }
```

**網頁版存在 `localStorage`，鍵就叫 `custom_pets.json`** —— 內容與桌面版那個檔一模一樣，
所以兩邊的資料可以互相搬（`custom.rs` 的 `the_desktop_file_format_still_loads` 釘住這件事）。
存取路徑抽成 `custom::Store` trait，`petcore` 只認 trait，不知道底下是檔案還是瀏覽器。

⚠️ **localStorage 會被「清除瀏覽資料」清掉**，桌面版至少還有個檔躺在硬碟上。
所以介面上留了 `exportCustom` / `importCustom` 讓使用者自己備份，倒出來的就是上面那份 JSON。

### 2.3 三層圖鑑

鍵一律是**名字**，不是編號（內建表有 483 筆沒編號，CSV 更是一筆編號都沒有）。
由下往上疊，`Library::rebuild` 每次都從最底下重疊一次：

| 層         | 來源                                 | 筆數     | 寫得了嗎                |
| ---------- | ------------------------------------ | -------- | ----------------------- |
| ① 內建     | 編進 wasm 的 TSV（`petdata`）        | 1200+    | ❌ 永遠在，唯讀         |
| ② 線上上游 | originmood CSV，開網頁時抓           | 1069     | ❌ 唯讀，抓不到就沒這層 |
| ③ 自製     | `localStorage` 的 `custom_pets.json` | 使用者的 | ✅                      |

① → ② 是 **`merge_upstream`：聯集，同名以 CSV 為準**。
CSV **不是**內建表的超集（內建有 200 多隻它沒有的），所以不能直接取代 ——
兩邊揉成一張完整的表才不會弄丟寵物。編號是唯一的例外：CSV 那邊沒有編號時**留著舊的**。

② → ③ 是 **`overlay`**：

| 動作             | 結果                                                   |
| ---------------- | ------------------------------------------------------ |
| 添加寵物         | 新增一筆自製記錄                                       |
| 修改**上游**寵物 | 存成一筆同名自製記錄**蓋在上面**，上游一個位元組都不動 |
| 刪除覆寫         | 自製那筆消失，**上游那隻露回來**                       |
| 刪除純自製       | 整隻消失                                               |
| 重載線上圖鑑     | 自製那層原封不動疊回去                                 |

編號**不自動配**：那是原程式動畫資源（`C{N}`）的鍵，自己加的寵物沒有對應動畫，
編一個假的只會誤導。原程式的編輯畫面本來也沒有編號欄位。

---

## 3. 計算邏輯 ⭐

**這一節是整個專案的規格。動這裡的任何數字都會讓測試變紅，那是刻意的。**

### 3.1 BP → 能力值（正向計算）

寵物所有能力由 5 個 BP 決定：`hpp(體力) attackp(力量) defendp(強度) agip(速度) mpp(魔法)`。
血魔攻防敏基本值各 20，精神／回復基本值各 100。

|       | 生命 | 魔力 | 攻擊 | 防禦 | 敏捷 | 精神 | 回復 |
| ----- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| +體力 | 8    | 1    | 0.2  | 0.2  | 0.1  | −0.3 | 0.8  |
| +力量 | 2    | 2    | 2.7  | 0.3  | 0.2  | −0.1 | −0.1 |
| +強度 | 3    | 2    | 0.3  | 3    | 0.2  | 0.2  | −0.1 |
| +速度 | 3    | 2    | 0.3  | 0.3  | 2    | −0.1 | 0.2  |
| +魔法 | 1    | 10   | 0.2  | 0.2  | 0.1  | 0.8  | −0.3 |

取整方式（**必須完全照抄，否則邊界會差 1**）：

```rust
fn fix(n: f64) -> i64 { ((n * 10000.0).round() / 10000.0).floor() as i64 }
// stat = fix(dot(row, bp)) + 20   （精神/回復 +100）
```

### 3.2 成長係數表 `fullRates`

某等級的 BP 成長：`bp[i] = 檔次[i] * bprate + fullRates[檔次[i]] * (lvl - 1) + bprate * random[i] + manual[i]`
其中 `random` 是總和為 10 的 5 元組（隨機檔），`manual` 是玩家加點。

111 筆，index 0–110，最大值 4.615：

```
  0: 0        1: 0.04     2: 0.08     3: 0.12     4: 0.16
  5: 0.205    6: 0.25     7: 0.29     8: 0.33     9: 0.37
 10: 0.415   11: 0.46    12: 0.5     13: 0.54    14: 0.58
 15: 0.625   16: 0.67    17: 0.71    18: 0.75    19: 0.79
 20: 0.835   21: 0.88    22: 0.92    23: 0.96    24: 1
 25: 1.045   26: 1.09    27: 1.13    28: 1.17    29: 1.21
 30: 1.255   31: 1.3     32: 1.34    33: 1.38    34: 1.42
 35: 1.465   36: 1.51    37: 1.55    38: 1.59    39: 1.63
 40: 1.675   41: 1.72    42: 1.76    43: 1.8     44: 1.84
 45: 1.885   46: 1.93    47: 1.97    48: 2.01    49: 2.05
 50: 2.095   51: 2.14    52: 2.18    53: 2.22    54: 2.26
 55: 2.305   56: 2.35    57: 2.39    58: 2.43    59: 2.47
 60: 2.515   61: 2.56    62: 2.6     63: 2.64    64: 2.68
 65: 2.725   66: 2.77    67: 2.81    68: 2.85    69: 2.89
 70: 2.935   71: 2.98    72: 3.02    73: 3.06    74: 3.1
 75: 3.145   76: 3.19    77: 3.23    78: 3.27    79: 3.31
 80: 3.355   81: 3.4     82: 3.44    83: 3.48    84: 3.52
 85: 3.565   86: 3.61    87: 3.65    88: 3.69    89: 3.73
 90: 3.775   91: 3.82    92: 3.86    93: 3.9     94: 3.94
 95: 3.985   96: 4.03    97: 4.07    98: 4.11    99: 4.15
100: 4.195  101: 4.24   102: 4.28   103: 4.32   104: 4.36
105: 4.405  106: 4.45   107: 4.49   108: 4.53   109: 4.57
110: 4.615
```

閉合式（與表完全一致，但**請直接硬編表**以避免浮點漂移）：

```
fullRates[n] = 0.04*n + 0.01*floor((n-1)/5) + (n>0 && n%5==0 ? 0.005 : 0)
```

> ⚠️ `n=0` 要特判：`floor((0-1)/5) = -1`，不擋會得到 `-0.01`。

#### cg-pet-calc 的表已依此修正 ✅

`Utils.mjs` 的 `fullRates` 原本只到 index 52，且 `51: 2.04, 52: 2.08` 是錯的
（比 `50: 2.095` 還小，破壞原表的單調遞增）；檔次 > 52 直接查到 `undefined`，
整條計算變成 `NaN`。已改成上面這張完整的 111 筆表：

| 檔案                            | 改動                                                                                       |
| ------------------------------- | ------------------------------------------------------------------------------------------ |
| `cg-pet-calc/lib/Utils.mjs`     | `fullRates` 補到 index 110、修正 51/52；新增並匯出 `fullRatesFormula()`                    |
| `cg-pet-calc/test/fullRates.js` | 新增 7 個回歸測試（筆數／單調性／閉合式／51-52 定值／表尾／高檔次不為 NaN／檔次-能力單調） |

因此 **cg-pet-calc 現在可以當作全檔次範圍 0..110 的差分基準**，
`crates/petcalc/tests/cross_check.rs` 已據此把對照範圍從 0..50 擴到 0..110
（1200 筆向量，`tools/gen/gen_vectors.mjs` 產生）。

### 3.3 反推（推算檔次）

`cg-pet-calc` 的 `GrowRange.guess()` 策略，移植時保留：

1. `loopRange()` — 每軸從 `檔次-4` 掃到 `檔次`（掉檔最多 4）＝ 5⁵ = 3125 組候選
2. 對每組候選：`calcBPAtLevel()` 算出基礎 BP，用總 BP 區間 `[oSum, oUpSum]` 先剪枝
3. 列舉手動加點 `(a,b,c,d,e)`，總和 = `lvl-1-notorderpoint`，各軸有 softLimit
4. 用反矩陣 `M_INV` 代數估計隨機檔 `r`，再在 ±1 鄰域列舉，逐軸驗證 5 個能力值
5. 整數列舉無解時，cg-pet-calc 會用循環投影做連續可行性回退
   —— **移植版不需要這條**，容差模型（§3.4）直接接住了

`M_INV`（`M_BP` 的反矩陣，用於代數估計）：

```
[ 0.13249019, -0.00806319, -0.06785930, -0.10938981, -0.16408472]
[-0.00782811, -0.00608853,  0.38607709, -0.02429143, -0.03643715]
[-0.00695832, -0.00541203, -0.02719073,  0.34877798, -0.03238858]
[-0.00467806, -0.00363849, -0.02954152, -0.02452651,  0.51876579]
[-0.00935612,  0.10383413, -0.05908304, -0.04905303, -0.07357954]
```

### 3.4 ⭐ 比對容差 — cg-pet-calc 沒有的關鍵

```
tol = clamp((當前等級 - 入手等級) / 200.0, 0.01, 0.1)
```

上下界 `0.01` / `0.1` 是原程式裡的 x87 80-bit 常數，已解出並驗證；
`hi`/`lo` 兩個全域的語意也已解出（見下）。

> ### ⚠️ 2026-07 更正：這裡原本寫錯了
>
> 舊版寫「原程式比對**能力值**時用帶容差的浮點比較」「原程式是把容差用在**區間
> 二分搜尋**上」。**兩句都是錯的**，是移植初期看反編譯看岔了，這次把 `setzjsx`
> 與建表函式整個讀完才確認。正確的是：
>
> 1. **原程式沒有二分搜尋。** 建表函式是 **13 層巢狀 `for`**（分組 5 + 4 + 4：
>    五軸檔次，加點與隨機檔各 4 個自由度），跟 cg-pet-calc／移植版**同一類作法**。
>    收到 1000 筆就停止存明細（只繼續累計筆數與逐軸 min/max）。
> 2. **能力值比對是精確的**，用 `Trunc`，七個欄位一個容差都沒有。
> 3. **容差只出現在檔次比對上**，而且只在 `檔次 mod 5 ∈ {0, 1}` 時才放寬。
>
> 推導過程與位址在本機的 `re/REVERSE.md` §E.3。

#### 容差的實際規則

比對用 `Trunc`（不是四捨五入）。對每一軸，看該軸**檔次**落在 `fullRates`
五週期的哪個位置決定要試幾次：

| `檔次 mod 5` | `fullRates` 在這裡的狀況            | 允許的偏移量      |
| ------------ | ----------------------------------- | ----------------- |
| 2, 3, 4      | 規律                                | `{0}`             |
| **1**        | `floor((n-1)/5)` 剛跳一階，誤差單向 | `{0, −tol}`       |
| **0**（n>1） | 多了 `+0.005` 半階，值卡在整數邊界  | `{0, −tol, +tol}` |

對照 §3.2 的閉合式 `fullRates[n] = 0.04n + 0.01·floor((n-1)/5) + (n%5==0 ? 0.005 : 0)`
—— **容差不是模糊比對，是針對成長係數表 5 週期不規則處做的 `Trunc` 邊界修補**。
會出問題的那兩個餘數才放寬，其他一律精確。

#### 移植版怎麼用

| 模式                  | 行為                                                           |
| --------------------- | -------------------------------------------------------------- |
| `MatchMode::Observer` | 上表的規則 ＋ 等級差容差 —— **這條才是原程式，也是預設值**     |
| `MatchMode::Exact`    | 取整後整數相等（cg-pet-calc 的作法，對拍測試走的就是這條）     |
| `MatchMode::Tolerant` | 全軸一律 ±`TOL_MAX`。**移植版自己的延伸，不是原程式的作法**    |
| `MatchMode::Auto`     | 由緊到鬆試 `Exact` → `Observer` → `Tolerant`，第一個有解的就用 |

⭐ **預設是 `Observer`，不是 `Exact` 也不是 `Auto`。** 這個專案的前提是
「行為跟原程式一模一樣」，那沒指定模式時就該走原程式那條 —— 預設成 `Auto`
（開頭先試 `Exact`）等於預設表現得像 cg-pet-calc，方向是反的。
petcalc 的 `MatchMode`、petcore 的 `MatchModeDto`、前端 `app.js` 的初始 state
**三處都是 `Observer`**，不讓任何一層偷偷不一樣。

> 改預設時踩出一個真的 bug：`GuessOptions::limit` 原本只在「換下一組（檔次, 加點）」
> 時檢查，而同一組底下可以命中好幾組隨機檔 —— 一次全推進去就超過上限（`limit: 5`
> 拿到 7 組）。`Exact` 命中少剛好不會超，所以預設是 `Auto`（先走 `Exact`）的時候
> 一直沒人發現。已改成在 `emit_random` 的 `try_tuple` 裡擋，
> `limit_caps_the_result_count` 現在四個模式 × 三種上限都掃。
> 應用層一律傳 `limit: 0`（不設限，機率統計要全集），所以跑起來的程式沒受影響。

`Tolerant` 以前被標成「原程式的作法」，現在改標成延伸並保留 ——
它比 `Observer` 寬，拿來當「真的推不出來時最後試一次」還是有用的。

移植時曾假設 `hi-lo` 就是整數能力格的寬度 1（→ `1/200 = 0.005` → 取下限 0.01）。
**這個假設被實測推翻**：拿 cg-pet-calc 測試套件裡的真實案例
「純白液態史萊姆 59 級」（檔次 `50,32,15,17,10`，`notorderpoint=13`），
窮舉所有 `(檔次, 加點, 隨機檔)` 整數組合後，**最小可達的最大單項誤差是 0.07**
—— 遠大於 0.01，但仍在上界 0.1 之內。現在知道 `hi-lo` 是等級差，
59 級野寵的 `59/200 = 0.295` 一樣會被夾到上界 0.1，兩邊結論一致。

#### cg-pet-calc 也加了這個模式 ✅

同一套規則獨立實作了一份到 cg-pet-calc，走 `guess(stat, notorderpoint, targetGrow, opts)`
的 `opts.mode`（`'exact'` 預設 ｜ `'observer'`）＋ `opts.catchLvl`，
`RealGuess` / `RealGuessRaw` 最後多收一個 `opts` 轉進去。

| 檔案                           | 改動                                                                                        |
| ------------------------------ | ------------------------------------------------------------------------------------------- |
| `cg-pet-calc/lib/Pets.mjs`     | `observerTolerance` / `tierNudges` / `observerMatch` / `nudgeSlack`；比對路徑分岔；四項匯出 |
| `cg-pet-calc/test/observer.js` | 4 個回歸測試（容差夾取／偏移規則綁 `fullRates`／superset／預設不變）                        |

⚠️ **加了偏移就得同時放寬剪枝**，否則靠偏移才成立的解在進到比對之前就被砍掉了。
偏移加在 **BP** 上，傳到能力值會被正算矩陣放大（生命那一列的係數和是 17），
所以寬放量不是 `tol` 本身，而是「該列在**可偏移的那幾軸**上的係數絕對值和 × tol」
（總 BP 那道則是 `tol × 可偏移軸數`）。兩邊都這樣改：Rust 在
`Matcher::{stat_slack, sum_slack}`，JS 在 `nudgeSlack()`。

兩邊各自列舉、各自取整，**解數逐案例相同**：

| 案例                | Exact | Observer | 其中靠偏移 |
| ------------------- | ----- | -------- | ---------- |
| 聖誕水藍鼠 lv98     | 3     | 16       | 13         |
| 純白液態史萊姆 lv59 | 0     | 4        | 4          |

Rust 側的數字由 `guess_check.rs::the_observer_rule_never_loses_an_exact_solution`
印出來（`-- --nocapture`），JS 側釘在 `test/observer.js` 的 `CASES` 表裡。

> cg-pet-calc **沒有** `Tolerant` —— 那本來就是移植版自己的延伸，
> 不該混進一個以「精確列舉」為賣點的函式庫裡。

### 3.5 候選表權重與機率統計

**權重 ＝ 隨機檔 5 元組的多項式係數**：

```
w(r) = 10! / (r₀! r₁! r₂! r₃! r₄!)
```

原程式拆成四個二項式連乘（`C(r₄,r₄)=1` 故省略第五項），隨後除以總和再乘 100
—— 所以存進表裡的已經是百分比。

> 這等價於「10 點各自獨立均勻落進 5 個軸」的多項分布。
> 地基不變量：全部 1001 組元組的權重加起來剛好是 `5¹⁰`
> （測試 `weights_sum_to_five_to_the_tenth_over_all_tuples`）。

原程式的候選表每列固定配置 `0x17d = 381` 個 slot ——
那正是「每軸掉 0..4 檔、總掉檔 10」的組合數，是**掉檔上限確實是 4** 的獨立佐證
（測試 `the_binary_s_381_slot_allocation_is_the_loss_tuple_count`）。

原程式從這張表出兩個東西：

- **逐軸邊際分布** —— 對每筆候選、每個軸：`acc[軸][掉檔量] += 權重`，得到 5×5 表。
  若某軸有一格 ≈100%（`100 - p < 0.01`）代表該軸已確定；五軸全確定就不顯示明細。
- **過濾後的機率總和** —— 五組 `(min, max)` 範圍全部落在範圍內的記錄權重總和，
  以及「三軸的加點成本合計 ≤ 門檻」的權重總和。

顯示門檻常數是 **0.01**（百分點），移植版 `stats::PERCENT_EPSILON`。

### 3.6 寵物搜尋的判定

「搜索不低於輸入能力的寵物」判定的是**補得到**而不是**天生就有**：算出該寵在目標
等級**不加點**的七項能力，每項的缺口除以該項最有效率那一軸的係數（`BEST_GAIN`，
全部來自 §3.1 的矩陣），合計 ≤ `等級-1` 就算命中。

⚠️ **只實作了單軸分支**。原程式另有一條 8:3 混加分支（`/0.6875` 進體力
＋ `×3/8` 進魔法，剛好讓精神不動），但選擇那條的變數來自哪個控制項沒查出來。
單軸是**下限**，所以不會漏掉本來該命中的寵物 —— 細節見 `search.rs` 檔頭。

---

## 4. 介面／操作邏輯

原程式**完全是 owner-drawn**：表單檔裡只有背景圖與隱藏的容器，中文字全部烘進
點陣圖裡。→ **不要**嘗試 1:1 復刻繪圖碼，用 HTML/CSS 重畫，保留**版面配置與操作
流程**（使用者稱讚的是「操作邏輯清楚好用」，不是像素）。

### ⛔ 4.0 介面嚴格照抄 asm —— 沒有理由，沒有藉口

**這一節底下的每一個控制項、每一條顯示規則、每一個欄位的語意與比對方式，
依據都必須是從反編譯讀出來的行為。** 不准拿下面這些東西當依據：

- 「這樣比較好用」「使用者大概會想要」「一般人會這樣填」
- 「原程式應該是這個意思」「照理說它會…」
- 「移植版的資料結構不一樣，所以只能這樣」

要動介面就先去讀 `re/REVERSE.md` 與反編譯，**沒讀到就不要寫**。

#### 三條硬規則

1. **查不到就不做。** 語意沒逆出來的控制項一律不補（例：「入手BP／當前BP」旁邊
   那兩個下拉、狩獵模擬的地圖係數）。在文件裡標成「語意未確認」，留著給下一輪。
2. **自己加的東西當場標明。** 移植版加的欄位／面板，**在程式碼註解與這份文件裡
   都要寫「原程式沒有這個」**。已經這樣標的有：逐軸 BP 那排（§4.3）、
   候選明細（§4.5）、隨機檔邊際分布、`MatchMode::Tolerant`。
3. **看錯了要留更正紀錄，不要默默改掉。** 把「原來寫的是什麼、為什麼是錯的、
   實際是什麼」寫進去 —— 這份文件的可信度是靠這些紀錄撐的，不是靠它看起來一致。

#### 為什麼要立這條

這份文件已經抓到過**八次**「把移植版自己的東西寫成原程式有的」：

| 位置             | 原本寫的                               | 實際                                           |
| ---------------- | -------------------------------------- | ---------------------------------------------- |
| §4.3             | 原程式的擴展頁也有逐軸 BP 那排         | 沒有，一格 BP 都沒印                           |
| §4.3             | 那張截圖的 BP 對應檔次 40 45…          | 那是**剛開起來的初始狀態**，畫面上沒有任何 BP  |
| §4.5             | 原程式把推算結果吐進 `TMemo`           | 那個 `TMemo` 在模擬視窗上，跟推算無關          |
| §6.3             | 12 個框是「7 欄能力＋5 欄檔次」        | 是「精神 回復 ＋ 逐軸 BP ×5 ＋ 檔次 ×5」       |
| §6.3             | 已確定的欄位**變灰**                   | 是**整欄隱藏**（送的是 `CM_VISIBLECHANGED`）   |
| §6.3             | BP 那格收小數，比對放寬 ±0.05          | 12 個 slot 都是 `int`，比的是 `Trunc` 後的整數 |
| §6.3             | 那 12 個是**輸入框**                   | 是**下拉**，值查表來的，一條解析文字的路都沒有 |
| §4.1 `style.css` | 分頁**跟著面板走**，「原程式就是這樣」 | 分頁**一格都不動**，動的是主視窗（見 §4.1）    |

八次都不是資料看不到，是**沒去看就先寫**。所以規則是「先讀 asm，再寫程式，
最後才寫文件」，不是反過來。

⚠️ 這三條特別值得記著：它們都有一段聽起來很順的理由（「變灰比較不會閃動」、
「使用者只抄得到一位小數」、「網頁上主視窗被推來推去很難看」）。
**理由越順，越要回去確認它是不是理由而不是依據。**

⚠️ 最後那一條還多示範了一件事：**編出來的規格會生出真的 bug。** 照那句寫出來的
CSS 讓分頁列在開合時橫移 `--side-w`、縱移 33px —— 使用者按完一顆分頁，那顆就從
指標底下跑掉，回報的原話是「我點擴展出現模擬 我選模擬出現擴展」「整個 tab 的
focus 狀態跟事件都怪怪的」。**沒有依據的那句話撐了幾個月才被使用者踩出來。**

### 4.1 主視窗

```
┌ 噬生・魔物觀測者                    [?] [−] ┐
│ 寵物資料  [📜] [寵物名字        ]  能力倍率  │
│ 最高檔次  [                    ]  [   20  ] │
│ 當前等級  [ 1 ][−][+]        [筒][保存][計算]│
│ 當前能力  [格式:血魔攻防敏(精神回復)        ]│
│ 運算模式  [智][野][無][體][力][防][敏][魔]  │
│          料理果然還是有個最美味的時間啊      │
└─────────────────────────────────────────────┘
```

左側直立分頁（可展開側欄）：**分 / 析 / 擴 / 展 / 模 / 擬**

⚠️ **一排的順序是 `[直立分頁][側欄][主視窗]`** —— 側欄往**左**開，
分頁貼在最左邊那塊的左緣、被它壓住一半。

#### ⭐ 分頁的行為（`showpanel`）

三顆分頁共用同一支處理器，`Sender` 的 `Tag` 就是分頁編號。流程照抄：

1. 先把七個面板**全部關掉**（分析／擴展／…）
2. 再看 `Tag` 跟「目前開著的分頁」比：

| 情況                       | 做什麼                                                     |
| -------------------------- | ---------------------------------------------------------- |
| 目前是收起的               | 展開（原程式靠 Timer 做滑出動畫，移植版沒有動畫）          |
| 點的是**目前那顆**且已展開 | **收起來**：按鈕彈起、「目前分頁」歸零、**一個面板都不開** |
| 點的是**別顆**且已展開     | 換頁：那顆按鈕壓下，只把對應的面板 `Visible := True`       |

⭐ **「點目前那顆會收起來」是原程式的行為，不是移植版的發明。** 使用者回報的
「分析點了自己不會 focus」講的就是這條 —— 它不是沒反應，是**收起來了**。

⭐ **分頁列本身一格都不會動。** 分頁按鈕跟主視窗不在同一個表單上（分頁畫在外殼那層），
收起那條路只對按鈕做「彈起 ＋ 重畫」，`SetLeft`／`SetTop` 一次都沒有；
搬的是**主視窗**（收起時 `Form2.Left := Form1.Left`、`Top := Form1.Top + $18`），
還有跟著它跑的 Form3／Form4。換頁那條路則**完全不動任何視窗位置**。

⚠️ **原程式那三顆拿不到焦點。** 它們是 owner-drawn 的圖片控制項（`TImageControl`
是 `TGraphicControl` 的後代，**沒有視窗 handle**），狀態只有彈起／壓下兩種。
移植版用 `<button>`，所以把滑鼠點完的殘留焦點框關掉，只留 `:focus-visible` 給鍵盤導覽。

⚠️ **有一處刻意沒照抄：收起時主視窗不會往左貼回去。** 原程式收起側欄後
`Form2.Left := Form1.Left`，主視窗會移到分頁列旁邊；移植版讓側欄那格永遠佔著
`--side-w`（收起來只是空著），主視窗留在原地。桌面上那是一個獨立視窗自己移動，
網頁上會變成整塊版面橫移半個畫面。**這是移植版的選擇，不是原程式的行為。**

#### 「當前能力」也吃整串的機器人指令（移植版加的）

Discord 那隻跑 cg-pet-calc 的「/算檔」機器人，指令是 `名字 等級(可省略) 血 魔 攻 防 敏`。
這格照樣貼得進去 —— 認出開頭的寵物名就順手把**檔次、能力倍率、等級**一起帶進來，
不必再去圖鑑點一次。原程式沒有這個，但它只是在既有的寬鬆解析器前面多接一段，
**不帶圖鑑時的行為逐字不變**（`parseStats` 的第二個參數不給就完全走舊路）。

兩個地方看起來像小事，其實是這功能唯一的難處（都寫在 `parse.js` 檔頭）：

| 坑                         | 為什麼                                                                                                                                                          |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **名字不能切到第一個數字** | 圖鑑裡有 `22週年鼠王`、`機甲D4型`、`第16期金怪` —— 切下去會得到空字串或 `機甲D`。走**最長的完全相符前綴**，這也正好落實「要完全相符才算數」，認不出來就當沒名字 |
| **等級靠數字個數判斷**     | 能力本體是 5 個或 7 個（多精神回復），都是奇數 → **6 或 8 個時第一個才是等級**，不會有歧義                                                                      |

⚠️ **省略等級 ＝ 1，不是「沿用現在那格」。** 沿用的話，上一次查 60 級留下來的數字
會悄悄套到這次查詢上。等級只在**認出寵物名的那一刻**被覆寫，之後手動改不會被蓋回去
（`app.js` 那個 effect 的相依只掛 `parsed.pet` 與 `parsed.lvl`，是刻意的）。

「入手能力」那格**不帶圖鑑** —— 它講的是同一隻寵物的另一個時間點，寫名字沒有意義。

### 4.2 運算模式（八選一）

| 鍵             | 含義                                                           |
| -------------- | -------------------------------------------------------------- |
| 智             | 家寵，等級點自由分配（可搭配側欄指定加點／混加）               |
| **野**         | **野寵：無手動加點，等級點由系統隨機分配**（配合「入手等級」） |
| 無             | 完全未加點（`lvl-1` 點全未配）                                 |
| 體/力/防/敏/魔 | 家寵，每升一級都固定加在該屬性                                 |

### 4.3 側欄面板

**分析 / 擴展**

- `入手等級` [n] [−][+] ＋ `使用入手能力` / `不用入手能力` 切換
- `能力無效` [文字]
- `加點方式`：`指定加點` | `混加`
    - 指定加點 → 5 格數值（體 力 防 敏 魔）
    - 混加 → 6 格範圍（體力 力量 強度 速度 魔法 計數）
- `搜尋範圍`（預設收合）→ `掉檔下限/上限`、`隨機檔下限/上限` 各 5 格 ＋ `還原`

> **`搜尋範圍` 在原程式裡是獨立的一頁**，兩欄各填一整串 `上限;下限`：
> `檔次範圍 4 4 4 4 4;0 0 0 0 0`、`隨機檔範圍 0 0 0 0 0;10 10 10 10 10`，
> 底下掛一句「如果你不知道自己在做什麼，請不要改變這裡的設定」。
>
> 移植版折進「分析」頁收起來放 —— **直立分頁是圖檔 sprite，原程式只畫了三個**，
> 加第四個分頁得憑空造圖。而這幾個數字跟同一頁的入手能力／加點方式本來就同一類：
> 都是拿來縮小推算解空間的約束。
>
> ⚠️ 預設值就是先前寫死的搜尋寬度（`LossBounds::default` ＝ 每軸 0..=4、
> `RandomBounds::default` ＝ 每軸 0..=10），**不碰它時與加這個欄位之前逐位元相同**
> —— `wire_check.rs::omitting_the_search_ranges_matches_spelling_out_the_defaults` 釘住這件事。
> 掉檔上限另外加了一道原程式沒有的護欄 `MAX_LOSS_BOUND = 9`：上限每加 1，檔次候選
> 就多一個維度冪次（`(n+1)^5`），敞開讓人填 50 只會讓分頁像當掉一樣。
> 隨機檔範圍湊不出總和 10 時後端直接回錯（`GuessReq::check_ranges`），
> 不會假裝成「查無解」。

**展開**

- `入手等級` [n]、`能力係數: 0.045 0系`（顯示該寵每等成長係數）
- `當前等級` [n]、`加點▼`、`暴點加點▼`
- `檔次` [0][0][0][0][0] [C] [模擬]
- `隨機檔` [2][2][2][2][2] [C] [復位]
- `暴點` 清單：下次 / 暴點:體力 / 力量 / 強度 / 速度 / 魔法

#### ⭐ 底下那排即時能力值一定要印逐軸 BP（**這排是移植版加的**）

> ⚠️ **這一節原本寫「原程式的擴展頁也有這一排」—— 那句是編的，已刪。**
> 對過截圖了（`orig/screenshot/…144425.png`）：原程式的擴展頁就是上面列的
> 入手等級／當前等級／檔次／隨機檔四列，**一格 BP 都沒印**。
> 這是這一串對話裡第三次把「移植版自己加的東西」說成原程式有的，所以特別標出來。

`results.js` 的 `StatStrip` 先前只印 `總BP` —— **總和是對不回任何畫面的**。
加逐軸那排的理由不是照抄原程式，是**對帳**：遊戲的「寵物狀態」視窗顯示的正是那
五個數（`體力 8.4 力量 9.4 強度 2.4 …`），所以逐軸那排是唯一能直接跟遊戲比對的東西。
`ForwardRow.bp` 這個欄位一直都在，只是前端沒畫。

小數取 1 位是照**遊戲**的顯示 —— 這格的用途就是拿去對，跟得緊的那個才有用。
能力值本身仍然走 §3.1 的 `fix()` 取整，公式一個字沒動。

實測釘住這件事：小白鴨（檔次 `40 45 10 20 10`、倍率 20、隨機檔 `2 2 2 2 2`、1 級）
→ BP `8.4 9.4 2.4 4.4 2.4`（合計 27.00）→ 能力 `128 84 49 33 32 98 105`，
與遊戲的寵物狀態視窗**七項全中**。

#### ⚠️ 原程式「檔次」那排的語意還沒確認

> ⚠️ 這段原本寫「同一畫面的 BP 又對應到檔次 `40 45 10 20 10`」—— **那也是編的**，
> 同一次修正時一起刪掉。那張截圖是**剛開起來的初始狀態**：主視窗的最高檔次全空、
> 當前能力還是提示字，畫面上根本沒有任何 BP 可以對。

手上唯一那張擴展頁截圖，`檔次` 是 `0 0 0 0 0`、`隨機檔` 是 `2 2 2 2 2`
（後者剛好等於移植版的 `DEFAULT_RANDOM`），而主視窗是空的 —— 也就是說
**那兩排就是預設值，看不出語意**。`檔次` 要嘛是**掉檔**（0 ＝ 一檔沒掉），
要嘛是「留空就吃主視窗最高檔次」的絕對欄，**一張初始狀態的截圖分不出來，不要用猜的**。

移植版目前走**絕對檔次**（`pickPet` 從圖鑑帶進來），另外自己多算一排唯讀的「掉檔」。
要不要改，得先在原程式那排填個非 0 進去，看底下的數字會不會跟著變。

兩個容易誤解的欄位：

- **「暴點加點」在原程式裡只寫不讀** —— 存進去之後再也沒有人讀它，語意無從還原。
  移植版重新定義成「從下一級起改加這一軸」，並在 `dto.rs::ForwardReq::burst`
  與 `panels.js` 兩處註明**這不是複刻**。
- **「入手能力」是真的約束**：入手當下 `manual = [0;5]`，所以那串能力只由
  （實際檔次, 隨機檔）決定 → `GuessOptions::catch`，常把隨機檔壓到唯一。

### 4.4 寵物檔案視窗

三個畫面互相取代：

- **清單** — 種族分頁 ＋ 清單 ＋ 名稱搜尋 ＋ 來源說明與「重載圖鑑」
    - 分頁順序照原程式（`petdata::RACE_TAB_ORDER`），**名字**用 §2.1 那套：
      野獸 不死 特殊 飛行 植物 人形 昆蟲 龍 金屬 其他
    - 原程式那裡是「圖鑑切換」下拉，移植版沒有多份圖鑑可切（只有一個線上來源），
      所以換成來源字串 ＋ 一顆重載鈕 —— 開網頁時已經自動抓過，那顆是**抓失敗時的退路**
- **編輯** — `魔物名字`、體力/力量/強度/速度/魔法、`能力倍率`、地/水/火/風、`種族`、`技能`；
  動作 `保存` / `放棄` / `刪除`
- **能力搜尋** — 原程式的輸入欄掛在主視窗，移植版搬進來跟結果放一起
  （輸入與結果隔著兩個視窗的話，改一個數字要來回看兩邊）

### 4.5 ⭐ 推算結果怎麼呈現 —— 原程式**不列候選解**

> **2026-07 更正（第一次）。** `results.js` 的檔頭原本寫「原程式是把結果吐進一個
> `TMemo`（純文字），這裡改成表格」。**那句是錯的** —— 唯一那個 `TMemo` 在「模擬」
> 視窗上，跟推算無關。原程式沒有「第 37 組解的加點與隨機檔長這樣」這種輸出，
> 那是 cg-pet-calc 的形狀。
>
> **2026-07 更正（第二次，使用者回報）。** 上面那句又矯枉過正了 ——
> 我接著寫成「原程式推算完畫面上只有兩樣東西」，把它的主結果欄整個漏掉。
> 使用者貼了實機截圖：主視窗的「計算結果」欄長這樣
>
> ```
> 計算結果  穩掉：2體 4防 3魔
> 13檔  -2 -2 -4 -4 -3  28.45%
> 14檔  -2 -1 -4 -4 -3  30.51%
> 14檔  -2 -3 -4 -4 -3  41.04%
> 單項掉檔概率(二次分析改) 0檔~4檔
> ```
>
> 三列的機率加起來剛好 100%，而且格式正好對上候選記錄的排版（§3.5：前五欄是
> `−lost[i]`、最後一欄是正規化後的百分比）。所以原程式**有**列東西，
> 列的是**掉檔組合**，不是完整候選解。

原程式推算完之後，畫面上有這些：

| 來源               | 內容                                                                               |
| ------------------ | ---------------------------------------------------------------------------------- |
| ⭐ **穩掉**        | 每軸「一定至少掉了幾檔」的一行摘要，掉 0 檔的軸不列                                |
| ⭐ **掉檔組合**    | 相異的掉檔 5 元組 ＋ 各自的機率總和，照總掉檔遞增排。71 組解會收成 3 列            |
| **逐軸邊際分布**   | 每軸「掉了幾檔」的加權機率（5×5，`showdet`）。某軸有一格 ≈100% ＝ 那軸已確定       |
| **過濾後機率總和** | 兩個 `%.3f` 的數字（`sumgailv`），配 12 個篩選框（7 欄能力 ＋ 5 欄檔次）縮小候選集 |

且**五軸全確定就連逐軸明細都不顯示** —— 都定死了還畫五排滿格長條圖是白畫。

#### ⭐ 穩掉 ≠ 「那一軸只剩一格」

這兩件事移植版一度混為一談，使用者回報的就是這個落差：

> 網頁我只看到總掉檔，穩掉只顯示掉了 4 防，沒說 2 體 3 魔

| 概念                           | 判準                       | 體力長條落在 2/3/4 時 |
| ------------------------------ | -------------------------- | --------------------- |
| `determined_lost`（`showdet`） | 某一格的機率 ≈100%         | **說不出話**          |
| `guaranteed_lost`（穩掉）      | 逐軸第一個權重不為零的格子 | 「至少掉 2 檔」       |

`determined_lost` 是 `guaranteed_lost` 的**子集**（定死的軸兩邊必然說同一個數，
由 `field_cases.rs::the_guaranteed_loss_is_the_per_axis_minimum_over_every_candidate` 釘住）。
只畫前者等於把「這一軸最少掉幾檔」這個確定的資訊丟掉。

#### ⭐ 掉檔組合：逐軸邊際同樣推不出來

理由跟總掉檔（見下）是同一個 —— **逐軸邊際沒有保留軸與軸之間的搭配**，
看得出「體力可能掉 2 或 3」卻看不出「掉 13 檔的時候那五個數字是哪一組」。
所以 `stats.rs` 在掃候選那一圈裡用 `BTreeMap` 另外收一份 `lost_combos`。

- 排序照原程式：**總掉檔遞增，同總掉檔時照元組遞增**
  （原程式那兩列 `14檔` 也是第二軸 1 在 3 前面，同一個順序）
- 相異組合上限是 `5^5 = 3125`，實測真實案例落在 13–260 之間，整份送進 DTO 不設限
- ⚠️ **符號跟原程式相反**：原程式存 `−lost` 也照那樣顯示（`-2 -2 -4 -4 -3`），
  移植版印正數。理由是同一塊面板裡的「候選明細」早就用正數了，
  一個面板兩張表對同一件事用相反符號比對齊原版更容易看錯。純顯示層的選擇，數值相同

移植版照這個資訊層級擺：

- **穩掉在最上面**（綠框一行 `穩掉 2體 4防 3魔`），接著**掉檔組合表**
  （捲動，最高機率那列標起來），這兩塊就是原程式結果欄的內容
- **邊際分布**跟在後面。全確定時收成一行 `體力 掉 0 檔 ｜ 力量 掉 0 檔 …`
  （這時穩掉與組合表都不畫 —— 只會有一組組合，solved 那行已經講完了）
- **候選明細收進 `<details>`**，標題直接寫「原程式沒有這一塊 —— 移植版加的」。
  沒整個拿掉是因為它真的有用（要看某一組完整加點就得靠它），而且它有 200 筆上限
  —— 標成附錄比擺在中央假裝是主角誠實
- **隨機檔的邊際分布是移植版加的**（引擎的 `random_marginal` 本來就算了，只是先前沒畫）。
  所以「全確定就收起來」那條**只套在掉檔上**：掉檔定死、隨機檔還有好幾種可能是常態，
  一起藏掉就是真的弄丟資訊，而原程式也沒有「藏隨機檔」這個行為可以照抄
- 「已確定」的門檻走 `Constants::percent_epsilon`（＝ `stats::PERCENT_EPSILON` ＝ 0.01），
  **不要在前端自己編一個 99.9**

#### ⭐ 總掉檔量：**推不出來，必須另外存一份**

「這隻總共掉了幾檔」是使用者最先想知道的一件事，而它**沒辦法從逐軸邊際算出來**：

> 逐軸邊際丟掉了軸與軸之間的相關性。各軸最小值的和只是總和的**下界**，
> 未必真的有哪一組候選同時取到那些值。

所以 `stats.rs` 在掃候選那一圈裡另外累一份 `lost_total_marginal`
（`[總掉檔 0..=20]` 的加權機率，`TOTAL_LOST_SLOTS = 5×4+1`），
範圍由 `lost_total_range()` 從權重不為零的格子取頭尾。

差距是實測出來的，不是理論擔心 —— `field_cases.rs` 的「衝浪小黃鴨 lv1」逐軸下界是 **1**，
真正的最小總掉檔是 **5**。兩條測試釘住這件事：

| 測試                                                        | 釘什麼                                                                      |
| ----------------------------------------------------------- | --------------------------------------------------------------------------- |
| `the_total_loss_range_agrees_with_scanning_every_candidate` | 加權統計 == 逐筆掃 `candidates[].lost_bp`（**期望值不是常數**，是掃出來的） |
| `the_per_axis_bound_is_looser_than_the_real_total`          | 真實資料上逐軸下界確實比真值鬆 —— 沒有這件事這個欄位就不必存在              |
| `per_axis_marginals_cannot_recover_the_total_range`         | `stats.rs` 裡的最小反例，講清楚為什麼                                       |

⚠️ **算總掉檔不能掃 `candidates`** —— 那個欄位在應用層有 200 筆上限（`truncated`）。
逐軸邊際不能用的理由跟這個無關（那是相關性問題），但兩個都指向同一個結論：
要對全集講話就得走 `distribution`。

前端三個地方吃它（`results.js`）：頂上的 `總掉檔 lo–hi` chip、邊際分布最前面那一塊，
以及候選明細裡「掉檔」那組多出來的 `總` 欄（那一欄用的是每筆候選自己的 `lost_bp`，
DTO 本來就有，只是先前沒畫）。

---

## 5. 開發約定

- 所有原始資料檔皆為 **Big5 / UTF-16-LE**，不是 UTF-8；**寫檔一律 UTF-8**
- Rust 計算核心必須有對照測試：以 `cg-pet-calc` 的輸出為 baseline，
  但 **`fullRates` 以本文件 §3.2 的完整表為準**（cg-pet-calc 該表有誤）
- ⚠️ **cg-pet-calc 不在 `package.json` 的相依裡**。它是 `file:../cg-pet-calc` 的本機路徑，
  CI 的 checkout 沒有那個目錄，留著 `npm ci` 會直接掛。對照向量已簽入
  `crates/petcalc/tests/vectors/`，所以 `cargo test` 與網頁建置都不需要它；
  要重產向量時自己 `npm i --no-save file:../cg-pet-calc`
- 逆向資料（`orig/` `re/`）**不進版控**，見檔頭的警告

### 5.1 指令

| 指令                     | 做什麼                                       |
| ------------------------ | -------------------------------------------- |
| `npm run dev`            | 建 wasm ＋ 開 Vite dev server                |
| `npm run build`          | 建 wasm ＋ 打包到 `dist/`（CI 跑的就是這條） |
| `npm run build:wasm`     | 只建 wasm → `wasm-pkg/`                      |
| `cargo test --workspace` | 全部測試                                     |

**沒有轉檔步驟了。** 圖鑑是開網頁時從上游抓的，不進版控也不進 `dist/`。

### 5.2 ⚠️ 動到 `package-lock.json` 時要用 npm ≥ 11

**這台開發機上的 `npm` 是 8.5.1**（`node` 倒是 v22），而 npm 8 有
[npm/cli#4828](https://github.com/npm/cli/issues/4828)：產 lockfile 時
**只記錄當下這台機器的平台**的 optional dependency。

結果是 —— 在 Windows 上重產 lockfile，裡面就只有
`@rollup/rollup-win32-*`，**沒有 `@rollup/rollup-linux-x64-gnu`**。
本機 `npm run build` 照跑，CI 的 `npm ci` 在 Linux 上就炸：

```
Error: Cannot find module @rollup/rollup-linux-x64-gnu
```

而且它**不會在 `cargo test` 那關被擋下來** —— 測試全綠、wasm 也建好了，
死在最後 `vite build` 那一步。第一次部署就是這樣掛的。

所以要重產 lockfile 一律走：

```bash
rm package-lock.json && npx --yes npm@11 install --package-lock-only
```

驗收方式是數 `@rollup` 的平台套件，**要有 25 筆**（含 `linux-x64-gnu`）：

```bash
grep -c '"node_modules/@rollup/rollup-' package-lock.json
```

只有 2 筆就是被 npm 8 產壞了，重來。

---

## 6. 移植進度

| #   | 項目                                                                                 | 狀態       |
| --- | ------------------------------------------------------------------------------------ | ---------- |
| 1   | **計算核心** `crates/petcalc` — `rates.rs` / `bp.rs` / `matrix.rs` / `grow.rs`       | ✅         |
| 2   | **推算引擎 ＋ 容差模型** `guess.rs` — `MatchMode::{Exact, Observer, Tolerant, Auto}` | ✅         |
| 3   | **候選表權重 ＋ 機率統計** `stats.rs`                                                | ✅         |
| 4   | **介面** — 主視窗 ＋ 直立分頁 ＋ 三塊側欄 ＋ 寵物檔案視窗                            | ✅         |
| 5   | **寵物搜尋** `search.rs` ＋ 圖鑑視窗第三個畫面                                       | ✅         |
| 6   | **網頁化 ＋ GitHub Pages** `crates/petweb`、`.github/workflows/pages.yml`            | ✅         |
| 7   | **推算搜尋範圍**（原程式的第四塊面板）`LossBounds` / `RandomBounds` ＋ 分析頁        | ✅         |
| 8   | **圖鑑換成線上 originmood CSV**（§2.1）＋ 三層疊合（§2.3）                           | ✅         |
| 9   | **推算後的再篩選**（原程式的「輸入更多資訊」）`RefineReq` ＋ `refine.js`             | ✅ 見 §6.3 |
| 10  | **原程式其餘面板**                                                                   | ⬜ 見 §6.2 |

對拍狀況：

- `tests/cross_check.rs` — 1200 筆向量，檔次 **0..110 全範圍**逐位元一致
- `tests/guess_check.rs` — 10 個案例（含 cg-pet-calc 測試套件裡的 3 個真實寵物），
  精確案例**解集合逐項相同**（最大一例 200 組解）
- `crates/petcore/tests/wire_check.rs` — 釘住 wasm 與原生的輸出**逐位元相同**
  （連 `27.269999999999996` 這種浮點尾巴都比對）
- `crates/petcore/tests/field_cases.rs` — **實際使用者查詢**的 7 個案例（來自 Discord
  跑 cg-pet-calc 的「/算檔」機器人），解的筆數與逐軸掉檔範圍全對，含一個「無解」案例

> ⚠️ 對機器人的輸出時有兩個坑，見 `field_cases.rs` 檔頭：
> 解 > 100 組時它印的筆數**被扣掉 10**（`Utils.mjs` 印 `results.length - limit`，
> 但那條分支根本沒列任何一筆），以及「總掉檔」是負的（`LostBP` 符號反了）。
> 兩個都是 cg-pet-calc 的顯示層問題，它自己的 `results.length` 跟移植版一致。
>
> 另外測掉檔範圍**不能掃 `candidates`** —— 那個欄位有 200 筆上限
> （`truncated: true`）。要用 `distribution.lost_marginal`，那才是對全集加權統計的。

### 6.1 不要照抄的東西

- 原程式的 INI key 是混淆過的 — 重製版用明碼
- 原程式的 null pointer bug（截圖裡出現過 access violation）
- Delphi `Extended`（80-bit）精度：Rust 用 `f64`。
  ⚠️ 需驗證邊界案例，原程式的捨入在 80-bit 下可能與 f64 有差異

### 6.2 剩餘面板的評估結果

| 面板             | 實際功能                                             | 判斷                                                 |
| ---------------- | ---------------------------------------------------- | ---------------------------------------------------- |
| 威力計算         | 解析空白分隔的技能數值字串 → 加總 → 2×7 矩陣乘       | 可做，但要先想清楚技能數值在移植版怎麼輸入           |
| 狩獵模擬（三頁） | 地圖係數是硬編 switch-case，**語意未確認**           | 先不碰                                               |
| 稱號收集         | 比對**原作者個人的**玩家 ID 清單                     | **不做** —— 那是作者自己的成就紀錄，不是通用遊戲資料 |
| `pprice`         | 表單裡有，但整份反編譯**找不到任何一處把它顯示出來** | **不做** —— 使用者看不到的東西不移植                 |

### 6.3 ✅ 推算後的再篩選 —— 原程式的「輸入更多資訊」

原程式推算完不是就結束了，它會回過頭來**要更多資訊，然後在同一批候選上再篩一次**。
移植版一度只做了輸出那半邊，輸入這半邊整個沒有，現在補齊了：

| 原程式的東西     | 是什麼                                   | 移植版                          |
| ---------------- | ---------------------------------------- | ------------------------------- |
| 逐軸邊際分布     | 每軸掉幾檔的加權機率（5×5）              | ✅ `results.js` 的 `Marginals`  |
| 掉檔組合         | 相異掉檔 5 元組 ＋ 機率                  | ✅ `results.js` 的 `LostCombos` |
| 過濾後機率       | 5 組 (min,max) ＋ 軸合計門檻 → 兩個 %.3f | ✅ `probability.js`             |
| **候選篩選**     | **12 個下拉**（哪 12 欄見下）            | ✅ `RefineReq` ＋ `refine.js`   |
| **跳過**         | 「這題我不知道」，直接略過這一輪追問     | ✅ 同上                         |
| **收窄搜尋範圍** | 篩完把掉檔範圍寫回「搜尋範圍」那一欄     | ✅ `narrowed_loss` → `app.js`   |

⭐ **關鍵差別是「什麼時候給約束」。** 側欄那些約束（入手等級／入手能力／加點方式／
搜尋範圍）都在**算之前**，資訊多了就得整個重算；這一塊是**算完之後在已建好的候選表
上再掃一遍**，不重算。完整候選與權重本來就留在 `GuessCache` 裡（`probability.js`
檔頭講的就是這件事），所以邊填邊看是即時的。

#### ⭐ 那 12 欄是哪 12 欄 —— 先前寫錯了

這裡原本寫「7 欄能力 ＋ 5 欄檔次」，**是錯的**。原程式那 12 個標籤的文字是編在
執行檔裡的字串常數，逐一解出來是：

| 欄     | 1    | 2    | 3–7                      | 8–12                     |
| ------ | ---- | ---- | ------------------------ | ------------------------ |
| 標籤   | 精神 | 回復 | 體力 力量 強度 速度 魔法 | 體力 力量 強度 速度 魔法 |
| 是什麼 | 能力 | 能力 | **逐軸 BP**              | **檔次**                 |

後兩組標籤一樣，靠**比對走哪條路**分辨。原程式的篩選是兩個相鄰的迴圈，
候選那邊各對到一段連續的 `double`：

| 欄       | 比對                                                               |
| -------- | ------------------------------------------------------------------ |
| **1–7**  | `Trunc(候選值) == 你填的整數`，**一個容差都沒有**                  |
| **8–12** | 同上，不中時再套檔次專用的那段 ±容差（`檔次 mod 5 ∈ {0,1}`，§3.4） |

容差那條路只有檔次會用（§3.4 講的就是檔次），所以 8–12 是檔次；剩下的 3–7
掛在「體力…魔法」五個標籤下、又不是檔次，只剩逐軸 BP。佐證是判斷「這一欄是否
已確定」時多出來的那條後路（值的十進位寫法超過 8 個字元就改比兩位小數）——
精神／回復是整數、檔次幾乎是整數，**會長出那種寫法的只有 BP**。

⭐ **那 12 個 slot 全部是 `int`（`-1` ＝ 沒填）。** BP 那五格也不例外，
問的是「BP 的整數部分是多少」。

#### ⭐ 那 12 個是**下拉**，不是輸入框

也弄錯過。原程式那 12 個控制項**沒有任何解析文字的路徑**：`OnChange` 拿到 Sender
之後只取出「我是第幾欄」，再配上一個「你選了第幾項」的全域，去查一張
`選項 × 欄` 的 `int` 表，查到的值才寫進篩選 slot。

旁證兩個：

1. 篩到 0 組時標籤變成「**選**錯」——不是「填錯」。那兩個字是執行檔裡的常數。
2. 「一開始就只有一個值的欄位根本不建控制項」這個行為，需要程式先知道每一欄有
   哪些相異值 —— 那正是選項清單。

選項表的填充碼沒找到，但內容只有一種填法說得通：**候選在該欄的相異值**。
少列一個，正確答案就選不到；多列一個，選了必然篩到空。移植版照這個填
（`RefineOptions`），**遞增排序是自己挑的**（原程式的順序沒逆出來）。

> 改成下拉之後，「BP 那格要收整數還是小數」這個問題自己消失了 ——
> 使用者選的是程式列出來的值，而列出來的就是比對用的那個量。

⭐ **生命 魔力 攻擊 防禦 敏捷 五格從來不在問題裡。** 它們是推算的輸入，早就釘死了。
移植版先前照著誤解畫了那五格，還得在旁邊解釋它們為什麼沒有自動變灰
（`Candidate.stat` 是**沒加偏移**那份 BP 反算的，容差命中的候選會差 1）。
那五格拿掉之後，那段解釋也跟著不必存在了。

#### 實作的形狀照原程式

| 原程式                                         | 移植版                                                |
| ---------------------------------------------- | ----------------------------------------------------- |
| 12 個下拉，`-1` ＝ 沒選                        | `<select>` ＋ `Option<..>`，留空 ＝ 不限              |
| 選項＝候選在該欄的相異值，建一次不再變         | `RefineOptions`，前端只取第一次那份                   |
| **一開始就只有一個值的欄位根本不建控制項**     | 拿第一次未篩的 `settled` 當基準，那幾欄不畫           |
| 某欄倖存候選全同值 → **標籤與下拉一起隱藏**    | 不畫那一格                                            |
| 使用者碰過的欄位不再隱藏（每次 OnChange 標記） | `touched`                                             |
| 篩到 0 組 → 剛動那格的**標籤變成「選錯」**     | 同（那兩個字是執行檔裡的字串常數）                    |
| 應答數 == 已確定數 → 收工                      | `RefineSettled::all`（只數上表那 12 欄）              |
| 「跳過」旗標 → 篩到空就退回全部候選            | `MoreInfo` 的 `skip()`：空集合先 `clear()` 再收起面板 |
| 篩完把逐軸掉檔範圍寫回搜尋範圍                 | `RefineResp::narrowed_loss` → `app.js::narrowRanges`  |

> ⚠️ 「已確定的欄位變灰」是**先前看錯的**：那支呼叫送的是 `CM_VISIBLECHANGED`，
> 是 `SetVisible` 不是 `SetEnabled`，而且標籤與輸入框是一起關掉的 ——
> 所以原程式的行為是**整欄從畫面上消失**。CSS 那條 `.field:disabled` 已經拿掉。

#### 「這一欄已確定了嗎」跟篩選是同一把尺

原程式判斷一致與否，主路就是比 `Trunc` 之後的整數 —— 跟那 12 個框比的是同一個量。
這件事讓面板收得了工：BP 那格填了 `8`，倖存候選的 `Trunc` 就都是 8，那一欄隨即消失。
`RefineSettled::bp` 因此是 `Option<i64>`（整數部分），不是 `Option<f64>`。

> ### ⚠️ 2026-07 更正：這裡原本寫「BP 比對放寬成 ±0.05」
>
> 舊版說「原程式要求兩邊的十進位寫法相同，但使用者只抄得到一位小數，
> 所以放寬成半格」。**那個放寬是自己編的**，理由再合理也還是編的（§4.0）。
> 重讀篩選那兩個迴圈才確認：12 個框都是 `int`，比對是 `Trunc(候選值) == 框裡的整數`，
> 前 7 欄連容差都沒有。輸入框現在收整數，小數點**打不進去**。

**有一條後路刻意沒有照抄。** 原程式判斷一致時，若值的十進位寫法超過 8 個字元，
會改拿「四捨五入到兩位小數」的字串去比 —— 那是給浮點雜訊用的補丁，而且它比主路
**嚴**：`93.835` 走主路得到 `93`，`93.83500000000001` 走後路得到 `93.84`，
兩筆幾乎相同的值會被判成不一致。不照抄的理由跟 §6.1 最後一條是同一件事 ——
原程式算在 80-bit `Extended` 上、移植版是 `f64`，**雜訊落在哪一筆本來就不一樣**，
照抄等於照抄一個平台相依的隨機行為。寫在 `dto.rs::RefineSettled` 的 doc 裡。

#### 其餘的差異

- **檔次那五格沒有容差。** 原程式的候選記錄裡檔次是**從 BP 反算的小數**，
  所以 `Trunc` 需要 §3.4 那套邊界修補；移植版的 `Candidate.grow` 是列舉出來的整數，
  修補已經在推算那一步做完了，整數相等就是對應的作法。
- **百分比對倖存候選重新正規化**（原程式不會）。被丟掉的那個資訊改用 `kept_percent`
  帶出來 —— 就是結果欄那顆 `已篩掉 N 組（留下的原本佔 X%）`。

⚠️ `refine.js` 那句 `touched.size > 0`（沒填過就不寫回搜尋範圍）**不是差異，是照抄**：
原程式那段寫回掛在輸入框的 `OnChange` 上，沒動過就不會執行。移植版第一次的空篩選
是自己排的（用來問出「哪幾欄值得問」），那不是使用者的動作，跟著寫回去才會變成
「按一下計算，側欄的搜尋範圍就被改掉」——那個才會是差異。

⚠️ **截圖裡「入手BP」「當前BP」旁邊那兩個下拉，語意沒有逆出來，不要用猜的補。**
下拉裡顯示「力量」「速度」的那兩個控制項在反編譯裡對不到讀取點。

---

## 7. 部署（GitHub Pages）

`.github/workflows/pages.yml`：push 到 `main` 就跑 `cargo test --workspace` → `npm ci`
→ `npm run build` → 上傳 `dist/`。**測試不綠就不會部署** —— 公式一動就會被擋下來。

幾個非顯而易見的設定，改動前先看清楚為什麼：

| 設定                            | 位置                | 為什麼                                                                                   |
| ------------------------------- | ------------------- | ---------------------------------------------------------------------------------------- |
| `base: './'`                    | `vite.config.js`    | Pages 的 project page 在 `/<repo>/` 子路徑底下，絕對路徑會 404                           |
| `worker.format: 'es'`           | 同上                | worker 用 `import` 載 wasm glue，classic worker 不支援                                   |
| `build.target: 'es2022'`        | 同上                | wasm-bindgen 產的 glue 有 top-level await                                                |
| `Serializer::json_compatible()` | `petweb/src/lib.rs` | `serde-wasm-bindgen` 預設把 map 序列化成 JS `Map`，前端 `row.stat.hp` 會拿到 `undefined` |

**圖鑑不在 `dist/` 裡。** 是使用者的瀏覽器跑起來之後直接抓上游的（§2.1），
所以 CI 沒有任何轉檔步驟，圖鑑更新也不需要重新部署 —— 上游改了，下次開網頁就是新的。

代價是**跨網域抓別人的 repo**：對方哪天改路徑、改檔名、或 raw 這個 host 擋起來，
圖鑑就只剩內建表。這件事不會讓程式壞掉（`bootstrap()` 吞掉那一步的錯誤），
但畫面上會少幾百隻，`console` 會有一行 `線上圖鑑載入失敗`。

---

## 8. 原作與素材

見 `README.md` 的「原作與素材」—— 原程式作者、cg-pet-calc、圖鑑來源的出處都在那裡。
`src/assets/ui/` 的 26 張介面素材是從原程式資源抽出來的，**有進版控**（建置時
CSS 直接 import），這件事在 README 與 `.gitignore` 兩處都寫明了。
