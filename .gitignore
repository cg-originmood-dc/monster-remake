# 建置產物
/dist/
/target/
/wasm-pkg/
node_modules/

# 抓圖與暫存
*.png.tmp

# 本機的暫存。圖鑑不進版控了 —— 現在是開網頁時直接抓上游那份 CSV，
# 要的是「永遠跟著上游走」，不是某次轉檔的快照。
/.cache/

# ── 逆向資料，不進版控 ─────────────────────────────────────────────
#
# 這個 repo 只放「重製出來的東西」：計算核心、介面。
# 原程式的執行檔、反編譯出來的東西、抽資源的腳本都留在本機。
#
#   orig/  monster.exe / petdll.dll / 原始 INI / 原程式截圖
#   re/    DFM dump、抽出來的點陣圖、逆向筆記（REVERSE.md）、抽取腳本
#
# ⚠️ 例外：`src/assets/ui/` 那 26 張圖是從原程式資源抽出來的介面素材，
#    **有進版控** —— 建置時 CSS 直接 import，拿掉介面就不成形。
#    出處說明見 README.md 的「原作與素材」。
/orig/
/re/
